import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  buttonBox: {
    flex: 1,
    flexWrap: 'nowrap',
    alignItems: 'center',
    margin: 5,
  },
})

export default styles
